# SETUP TAMBAHAN
1. Buat direktori C:/DEV
2. Setting environment set path C:/SERVER/PHP83 dan C:/SERVER/APACHE/bin
3. Install apache menggunakan CMD (Administrator) di folder C:/SERVER/APACHE/bin dengan perintah ```httpd -k install && httpd -k start```
